import { Button } from "./ui/button";

const DesktopNav = () => {
  return (
    <Button
      variant="ghost"
      className=" font-bold text-1xl hover:text-orange-500 hover:bg-white"
    >
      Log In
    </Button>
  );
};

export default DesktopNav;
